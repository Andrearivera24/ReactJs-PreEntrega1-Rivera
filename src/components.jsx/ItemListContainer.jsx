import {Text } from '@chakra-ui/react'

const ItemListContainer =({greeting}) =>{

   return    <div>
    
  
        <Text className='greeting' textAlign='center' fontSize='5xl'>{greeting}</Text>
            </div>

 };
 export default ItemListContainer;
